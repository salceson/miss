trafficsimulation {
  area {
    size = 2 // number of horizontal and vertical roads (total roads count is 2 * size)
    cells_between_intersections = 20 // must be even
    lanes = 3
    traffic_density = 0.4
  }
  vehicle {
    max_velocity = 3
    max_acceleration = 1
  }
}